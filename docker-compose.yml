services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      FRONTEND_ORIGIN: http://localhost:5173
  web:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - api
